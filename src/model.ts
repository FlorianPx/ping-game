type X = number;
type Y = number;
type RadiusType = number;
type ColorType = string;

type VelocityType = {
  x: X;
  y: Y;
};

type PlayerDataType = {
  score: number;
  bestScore: number;
  level: number;
};
